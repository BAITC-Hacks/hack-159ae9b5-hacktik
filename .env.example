# Optional AI intent parsing. Keep this file private after copying to .env.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5.4-mini
# For local use leave blank. Only set PUBLIC_ORIGIN when deploying behind HTTPS.
PUBLIC_ORIGIN=
# Optional absolute path to the persistent database.
EKT_DB_PATH=
